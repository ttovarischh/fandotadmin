import React from "react"
import PropTypes from "prop-types"

const A_CardUser = ({
textpart
}) => {

const checkStyle = STYLES.includes(style)
? style : STYLES[0]

return (
<div>
  <p>{textpart}</p>
</div>
);
}

export default A_CardUser
